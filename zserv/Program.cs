using System;

namespace zserv
{
	class MainClass
	{
		public static void Main (string[] args)
		{
			/* parse command line option
			 * -d deattach (start subprocess and tunnel output, exit on [deattached]
			 * -c config file
			 * -o global options
			 **/
		}
	}
}
